# Positive-control canary — DELETE ON SIGHT

This file exists to prove the secret-scanning gates can fail. It is a Sprint 17
test artefact and must never survive on `main`.

The value below is **worthless and fabricated**. It is not a credential, it does
not identify any record, and it grants access to nothing. It is shaped to match
the `airtable-id` rule in `.gitleaks.toml` and nothing more.

Canary: recAB12CD34EF56GH

If you are reading this outside the test branch, the cleanup step failed.
